/**
 * ? 1. e.target [name of the input field].value --> we will get the value
 *! use form action and formData in the action Handler --> formData.get('[name of the input field]')
 * TODO : Controlled Component one per each field for dynamic handle of the error a little disadvange is it becomes overloaded
 * ? Handling all Contolled Field on one state object
 * ? const [formDat , setFormDta] = [
 * ? {
 * ? name : '',
 * ? password : '',
 * ? Phone : ''
 * ?}];
 * ! Uncontrolled
 *
 *
 */
